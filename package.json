{
  "name": "mcservermanager",
  "version": "1.0.1",
  "description": "Minecraft Server Mangement App",
  "main": "app.js",
  "scripts": {
    "test": "electron .",
    "build": "electron-packager . 'mcservermanager' --platform linux --arch x64 --out dist/ --overwrite --icon=icon.png",
    "deb64": "electron-installer-debian --src 'dist/mcservermanager-linux-x64/' --dest dist/installers/ --arch amd64"
  },
  "author": "Dunemask",
  "license": "GPL2.1",
  "devDependencies": {
    "electron": "^10.1.1",
    "electron-installer-debian": "^3.1.0"
  },
  "dependencies": {
    "child_process": "^1.0.2",
    "tree-kill": "^1.2.2"
  }
}
